import CompanySelector from "../components/company-selector";

const Header = () => {
  return (
    <header className="header">
      <CompanySelector />
    </header>
  );
};

export default Header;
